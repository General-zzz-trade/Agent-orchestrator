version: "3.8"
services:
  agent:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - LLM_PLANNER_PROVIDER=${LLM_PLANNER_PROVIDER:-}
      - LLM_PLANNER_API_KEY=${LLM_PLANNER_API_KEY:-}
      - LLM_PLANNER_MODEL=${LLM_PLANNER_MODEL:-}
      - LLM_PLANNER_BASE_URL=${LLM_PLANNER_BASE_URL:-}
    volumes:
      - agent-data:/app/data
    restart: unless-stopped

volumes:
  agent-data:
